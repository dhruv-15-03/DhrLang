// My First DhrLang Program
// Following the Getting Started guide

class HelloWorld {
    static kaam main() {
        printLine("Hello, DhrLang! 🚀");
        printLine("This is my first program in this amazing language!");
        
        // Let me try some variables
        sab myName = "Sarah";
        num myAge = 28;
        kya loveProgramming = true;
        
        printLine("My name is " + myName);
        printLine("I am " + myAge + " years old");
        
        if (loveProgramming) {
            printLine("I love programming with DhrLang!");
        }
        
        // Let me try a loop
        printLine("Learning to count in DhrLang:");
        for (num i = 1; i <= 3; i++) {
            printLine("Number: " + i);
        }
        
        return;
    }
}