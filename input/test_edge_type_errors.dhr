// Purpose: Exercise type checker with diverse type errors
class TypeErrorSuite {
    static kaam main() {
        // Incompatible assignments
        num a = 10;
        sab b = 20;              // ERROR: assigning number to string
        kya c = "true";          // ERROR
        duo d = a + "x";         // ERROR: number + string
        num[] arr = [1, 2, 3];
        arr[0] = "oops";        // ERROR
        num notArray = 5;
        notArray[0] = 2;         // ERROR: indexing non-array
        // Function call arity/type errors
        num result = add(1, 2, 3); // ERROR: wrong arity
        num wrong = add("x", 2);  // ERROR: wrong type
    }
    static num add(num x, num y) { return x + y; }
}
