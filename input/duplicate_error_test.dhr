// INTENTIONAL NEGATIVE TEST: Triggers multiple compile-time errors for diagnostics (missing semicolon, unterminated string, unsupported multi-dimensional array).
class TestDuplicateErrors {
    static kaam main() {
        // Multiple error types to test
        
        // Syntax error 1: missing semicolon
        num x = 5
        
        // Syntax error 2: invalid expression
        num[] arr = [
        
        // Syntax error 3: invalid token
        sab text = "unclosed string
        
        // Syntax error 4: multi-dimensional array (known limitation)
        num[][] matrix = new num[3][3];
        
        printLine("This won't run");
    }
}
