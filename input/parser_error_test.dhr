// INTENTIONAL NEGATIVE TEST: Exercises parser error recovery (incomplete array literal, missing closing brace).
class ParserErrorTest {
    static kaam main() {
        // Missing semicolon - parser error
        num x = 5
        
        // Incomplete array literal - parser error  
        num[] arr = [1, 2
        
        // Missing closing brace - parser error
        kya condition = true;
        agar (condition) {
            printLine("test");
        // missing }
        
        printLine("end");
    }
}
